<p id="notice"><%= notice %></p>


      <div class= "ingredients-container">
        <div class="ingredients-display-ingredient-show">
          <% @ingredients.each do |ing| %>

          <%= link_to ingredient_path(ing) do %>

          <div class="ingredient-avatar-card">
            <% if ing.image.attached? %>
              <%= cl_image_tag ing.image.key, class: "ingredient-avatar" %>
              <% end %>
              <p><%= ing.name.capitalize %></p>

        <!--   (<%= ing.id %>) -->
          </div>

          <% end %>
          <% end %>
        </div>


      </div>


<!-- <h1>Ingredients</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Image</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @ingredients.each do |ingredient| %>
      <tr>
        <td><%= ingredient.name %></td>
        <% if ingredient.image.attached? %>
        <td><%= cl_image_tag ingredient.image.key, height: 300, width: 400, crop: :fill %></td>
        <% end %>

        <td><%= link_to 'Show', ingredient %></td>
        <td><%= link_to 'Edit', edit_ingredient_path(ingredient) %></td>
        <td><%= link_to 'Destroy', ingredient, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br> -->

<%= link_to 'New Ingredient', new_ingredient_path %>




